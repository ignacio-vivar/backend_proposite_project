# Run
Just create a virtual environment and install requirements.txt.

```shell
python3 -m venv venv
pip install -r requirements.txt
```

Then run: 
```python
python3 main.py
```

# Database
To populate database run:
```python
python3 loadDB.py
```